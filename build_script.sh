# Navigate to your project
cd /Users/goktugocal/Desktop/projects/GoktugOcal.github.io

# Install dependencies (first time only)
bundle install

# Build the site (generates static files in _site/)
bundle exec jekyll build

# Serve locally with live reload (development)
bundle exec jekyll serve

# # Serve with drafts included
# bundle exec jekyll serve --drafts

# # Build for production
# bundle exec jekyll build --livereload